This is a benchmarking program designed to evaluate 3 Linux schedulers:
    1. SCHED_OTHER
    2. SCHED_FIFO
    3. SCHED_RR


Prerequisites:

    gnuplot - plotting
    pandoc - only for report
    runhaskell - only for report
    pandoc-citeproc - only for report


Building and Running:

Build all executables:

    make

For more information on each executable use the -h flag as most support it.

Run benchmarks:

    make clean; make clean-results; make; make benchmark; make plot

Make report:

    make report

NOTE: Before the report is made the benchmarks must be run.
